# Known Issues

- The first KAVBAN dashboard uses seeded UI data.
- Project routes currently render the same KAVBAN dashboard instead of live project data.
- The Codex intake endpoint is specified by schema but not wired into the Rust server yet.
- KAVBAN metadata is not yet persisted in the local database.
- Human approval rules are documented but not yet enforced by merge actions.
